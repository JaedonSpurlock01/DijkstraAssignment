#include <iostream>
#include <vector>
#include <string>

// This is commented out for now to show how the parsing functions were tested
void test_parsing()
{
    // std::cout << "Testing outputs:\n";
    // std::cout << "Testing Parsing Cities:\n";
    // for (int i = 0; i < parsedCities.size(); i++)
    // {
    //     std::string id = parsedCities[i][0];
    //     std::string code = parsedCities[i][1];
    //     std::string name = parsedCities[i][2];
    //     std::string population = parsedCities[i][3];
    //     std::string elevation = parsedCities[i][4];

    //     std::cout << " ID: " << id << " CODE: " << code << " NAME: " << name << " POP: " << population << " ELE: " << elevation << std::endl;
    // }

    // std::cout << "Testing Parsing Routes\n";
    // for (int i = 0; i < parsedRoutes.size(); i++)
    // {
    //     unsigned from_city = parsedRoutes[i][0];
    //     unsigned to_city = parsedRoutes[i][1];
    //     unsigned distance = parsedRoutes[i][2];

    //     std::cout << "ROAD -> FROM: " << from_city << " TO: " << to_city << " DISTANCE: " << distance << std::endl;
    // }
}